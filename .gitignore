node_modules/
dist/
# electron-builder's output directory (see electron-builder.json) — the
# packaged app, the installer and their logs, all regenerated by `npm run
# build:win`. Committed once by accident, which put a 177 MB Threadline.exe
# into the history and made every push fail: GitHub rejects any file over
# 100 MB.
release/
.env
.env.local
.DS_Store
*.log
.vite/
